Internal todo list

- Add the things on this list to the feature request list?
- Check if explorer context menu entry is missing and
  ask the user to restore it?
- Better easier GUI
- Inline help by using speech bubbles or something like that
- Better detecting of locked and protected folders